# 当前开发 TODO（短入口）

更新：2026-09-27（第四轮 M 主控）。唯一状态源是[执行计划](development-execution-plan.md) 的 `current_next`（主干 integration `NEXT`；已划清独占路径的 lane 另读 §2 激活表里的 `lane_next`，`lane_next` 不构成主干 `NEXT`、也不写成主干 `DONE`）；本页仅便于看进度。此前 3601 行已完成事项、原始读数和 Qoder 未提交的第七类审计草稿保存在[历史 TODO](development-todo-history-through-cef712b.md)，没有丢弃。

## 当前

- [x] `DONE`（2026-09-26）：`V1201-LOCAL-DEMO-REHEARSAL-001`。全新 data root/全新 Kin 的一次自动路径 run 接成 sealed PASS：装机 3639/3639、同 generation 的 look+move 与到期释放、退出；四读一致，七类反证各自变红，两次失败 attempt 保留。读数与缺口见[执行计划 §2](development-execution-plan.md#2-上一卡交付与当前-next-边界)。
- [x] `DONE`（2026-09-26）：`V1201-LOCAL-NEGATIVE-MATRIX-001`。六族逐族量出当前 build 的可重跑拒止与 sealed 重读：18 行真 socket 状态探测 `FAIL=0`、自动入口两条 `NEEDS_PIN`、装载期禁区地址四拒（含"拒在发出字节之前"的连接计数证明）、显式路径五拒 + Bridge 钉错两拒、`V1201-060/070/080` 本 build `PASS / agrees`。逐行读数与配对反证见[负向矩阵复判记录](version-negative-matrix-2026-09-26.md)。
- [x] `DONE`（2026-09-26）：`V1201-TESTED-GATE-READOUT-001`。规范卷只读复判：provenance `verified / rc=0`、六条 1.20.1 引用四读一致且全归当前 build、19 能力与六 case 断言双向差集为空、promotion 三门仍 `false`（`CASE_VERSION_MISMATCH`/`NO_MANDATORY_CASES`）、卷内 15 次 attempt 的引用与失败分布已列清；四条反转（缺件/摘要改一位/空根/删工件）证明读者能判红。边界与读数见[复判记录](tested-gate-readout-2026-09-26.md)。
- [x] `DONE`（2026-09-26）：`P0-EVIDENCE-INVENTORY-001`。规范卷只读四桶盘点：`missing_fixture 31 / no_current_build_evidence 28 / real_failure_on_current_build 0 / current_build_pass 15`（74 条恰落一桶），11 门 block 与三种 block 语义分开，产品未实现只在缺 fixture 侧点名 6 条整条 + 2 个半句，HOST 18 条不判；四条反证各移一个机制（删 fixture / 空根 / 改判 `FAIL` / 改一位摘要），真件八条自检 `FAIL count = 0`。盘点不点亮任何门。逐条读数见[盘点记录](p0-evidence-inventory-2026-09-26.md)，排卡见[执行计划 §3.3](development-execution-plan.md#33-b1-交出的缺-case-排卡按-12-单独登记不由-b1-顺手写断言也不自占队列)。
- [x] `DONE`（2026-09-26，B2 的第一刀）：`P0-CORE-040-050-RUN-001`。`CORE-040`、`CORE-050` 各在**当前构建**上补了一轮受控本地 dedicated offline 真跑并封证：两条判绿行四读齐全（verdict / `evidence verify` / `report_promotion` row / 独立复判 `6/6`、`4/4`、`disagreements=[]`），`case_version` 逐字节等于今天的摘要、`from_repository_build: true`；W60 读数转为 `promotable: true / blocks []`（**仅机器候选，不晋级**），`p0-core` 只剩 `REQUIRED_CASE_NOT_REGISTERED`。`CORE-050` 第 1 次 attempt 因本次命令漏 `STILL=1` 真实 FAIL 并保留。反证四组（撤任一 run ⇒ 门红；改一位封进去的字节 ⇒ `EVIDENCE_NOT_VERIFIED` + `rc=12` + 复判拒判，规范卷不受影响）。读数见[第一刀记录](p0-core-040-050-run-2026-09-26.md)。
- [x] `DONE`（2026-09-26，B2 的第二刀）：`OFFLINE-030`（B2 卡面格，非独立卡号）。受控本地 dedicated offline 裸 join 一次真跑（`run-174` / run `3ea7c6b9…`）封出该 case 在本根上的**第一份**当前构建 PASS：四读齐全（`PASS / failures [] / attempt 1`、`evidence verify verified true rc=0`、`report_promotion row build=True verified=True AGREES crit_match=True`、独立复判 `agrees observed=1/1 disagreements=[]`），`case_version f3d834e93e93…` 逐字节等于今天的摘要，jar 实测 sha1/size 等于 `SERVER_RECIPES["1.21.4"]` 的 pin。**门一字未动**：该行 `mandatory: false`，从镜像副本撤掉它，W30/W60/`p0-core`/overall 四行与正对照逐字相同。判官非空转：同一份封进去的材料一次改一个事实，八行各得一个具名否（正对照 `PASS`）。顺带量到：今天 11 个包没有任何 `CASE_VERSION_MISMATCH`。读数见[第二刀记录](p0-offline-030-run-2026-09-26.md)。
- [x] `DONE`（2026-09-26，E lane 收 A2 的 N1）：`V1201-AUTO-ENTRY-GATE-ORDER-001`。实现已由 S lane 合入主干 `b88e36f`，本条记的是**收口读数**：在主干 `ed37260` 上按原脚本原期望重跑复判记录 §3 的三组行，X2 由 `exit=124 / store 388` 变为 `exit=17 / store 0 / 0 条 fetching`，Y2 由「整店复核之后才拒」变为「一步就拒」（同 store 的 Y1 仍穿过 join 授权 ⇒ 非空转对成立），B1/B2 由 `exit=124` 变为 3–4 秒具名 `ADMISSION`，十三行之后 store 由 290 文件变为 0 而正对照 C1 仍进入装机；反向对照用 `b88e36f` 的父提交 `300ed07` 重放 X 组得到旧读数。**不封 bundle、不动任何门**；B12/B13 两侧都因脚本端口表为空而未测到场景（要补需另卡）。读数与复现见[S1 门序重跑读数](s1-gate-order-rerun-2026-09-26.md)。
- [x] `DONE`（2026-09-26，B2 的第三刀）：`ADMIT-001/040/060/100/110` 五条（B2 卡面格，非独立卡号）。五轮受控本地真跑各封一份**当前构建** PASS bundle（`run-175/176/177/178` + 黑洞 run，`argv_digest` 逐字记录在案），四读齐全：verdict `PASS` / `evidence verify rc=0` / `report_promotion` 每行 `build=True verified=True sealed=True AGREES crit_match=True violations=0` / 独立复判 `2/2、6/6、6/6、3/3、3/3` 且 `disagreements=[]`。旋钮生效由 bundle 里的字节证明（`online-mode=true`、`require-resource-pack=true` + loopback pack URL 与 sha1、`snapshots_admitted=0`、无 `server/` 目录），不是叙述。规范卷底色 `attempts 54→59 / bundles 90→95 / from_another_build 61→61`，B1 那格「本根只有别的构建的证据」由 5 变 0。**门一字未动**（正对照 P 与撤掉五行的 R1 给出同一份门读数：W40/`p0-core` 仍 `REQUIRED_CASE_NOT_REGISTERED`、`satisfied: []`，W60 仍 `promotable: true`）；一位字节的 R2 使 `verify rc=12` + 复判 `ARTIFACT_DIGEST_MISMATCH`，同一条在规范卷上仍 `rc=0`。旧失败材料全留（`ADMIT-060 3c17aa78` FAIL+`DISAGREES`、`ADMIT-110 c383d4f1` FAIL）。读数见[第三刀记录](p0-admit-five-run-2026-09-26.md)。
- [x] `DONE`（2026-09-26，B2 的第四刀）：`CORE-030`（B2 卡面格，非独立卡号）。**第一条 LAN 形状的当前构建真跑**：`kin-01` 用 `--world-save/--world-name kinworld` 起客户端并把世界 publish 到 `25570`，`kin-04` 作 `Kin2` 拨号加入，加入者 bundle `19ff9064…` 封 10 件 artifact、判据 `2/2`、复判 `AGREES`；宿主文档的 `settings_digest 3bdd4aff…` 逐字节等于 case 钉住的 `level.dat`，判据第二半读的是**另一个 run 的字节**。字节级到达证明：宿主日志 `Started serving on 25570`→`Kin2 joined the game`，加入者日志 `bridge asked vanilla to connect to 127.0.0.1:25570`→`CONNECTION_PHASE_JOIN_SEEN`，终态 `PLAYABLE / snapshots_admitted 1 / entities_admitted 71 / PLAYER_EQUIVALENT`。规范卷底色 `attempts 59→61 / bundles 95→97 / from_another_build 61→61`，B1 §6 那格「本根只有别的构建的证据」由 3 变 2（只剩 deferred `HOST-030/040`），**B1 §7 定义的 9 条最小内容就此齐了**。**门一字未动**（P/R1/R2 三种卷状态下 `p0-core`/W40 仍 `REQUIRED_CASE_NOT_REGISTERED`、`satisfied: []`）；R3/R4 量到「移判据 ⇒ 两种判决都不给」（`unjudged rc=2`），named refusals 只能从未移判据读。**两次失败都留着**：第一次 `rc=2` 是 runner 确定性缺陷（`domain.sh:657` 在无 `run/session/` 的新 joiner Kin 上被 `pipefail` 终止且错误被 `2>/dev/null` 吞掉，已作为缺陷报告交主控，本卡未 patch 已提交代码），第二次加入者崩在 `GLFW [0x1000E]Failed to detect any supported platform`、封成 FAIL 并留下 crash report —— 环境块与成功那次逐字相同、`/proc` 探针证明被管客户端确实拿到 `DISPLAY=:99` + 有效 cookie、A–G 矩阵把四条能拒止的 X 路径各自命名，**一字未改重跑即 PASS ⇒ 记为不可重现，不编根因**。读数见[第四刀记录](p0-core030-lan-run-2026-09-26.md)。
- [x] `DONE`（2026-09-26，B2 的第五刀）：`OFFLINE-001/040/050` + `ADMIT-080` 四条（B2 卡面格，非独立卡号）。**第一条走「仓库自检」通道的刀**：判官只需仓库字节的 17 条本根零 bundle 行，本轮清空非 HOST 的 4 条（17 → **13**，剩下的 13 条**全部**属 `HOST*` 家族）。检查在宿主 `.venv`（pytest 9.1.1）跑、封存唯一一次对规范卷可写地跑在受控镜像里，四条各封一份当前构建（`launch_plan_digest bcc0c10d…`）PASS bundle：`935c034a… 5/5`、`4f324c20… 3/3`、`f4203033… 3/3`、`66c51fc7… 6/6`，attempt 各 seq 1，工件 9/7/7/10。四读齐全（封存自报 `sealed/PASS/failures []`；只读挂载上 `evidence verify` 四条 `rc=0`；`report_promotion` 每行 `build=True verified=True sealed=True violations= []`；复判端报 `UNJUDGED` + 既有的 asserter-inputs 理由 ⇒ 该通道第二次读法是**重跑检查**，四条各有全新 run id 的独立重跑 `c844cffa/5826ff69/25696021/e115a539` 并同意）。规范卷底色 `attempts 61→65 / bundles 97→101 / from_another_build 61`（一字未动）。**门一寸未动，而且是量出来的**：封前后各跑一次 `report_promotion.py`，`work_packages`+`overall` 整段 sha256 两次都是 `fb0152c85d029ee0…`。七件反证：P 原样副本列出 9 份、R-A/R-B 一位字节（检查输出与判决工件各一处）⇒ `rc=12` + `ARTIFACT_DIGEST_MISMATCH` 且**还原即回 `rc=0`**、R-C 规范卷同时仍 `rc=0`、R-D 改产品一字节 ⇒ `OFFLINE-001 3/5`、`OFFLINE-050 2/3`、`ADMIT-080 4/6` 与 `OFFLINE-040` 自己那一刀 `2/3` 全红并如实封成 FAIL（在卷外 `.tmp/b5-scratch-root`，失败材料留着）、R-E 删四个 run 目录 ⇒ `bundles 9→5` 而 `attempts` 仍 65。顺带量到两件交主控的事实：受控镜像里**没有 pytest**（`find_spec` → `None`，硬跑会得到四条 `No module named pytest` 的假 FAIL，本轮未 pip install/未改镜像/未封假 FAIL），以及台账里有 `SEALED` 行而 bundle 目录已不存在时 `unsealed/unreadable` 都是 **0**（半套卷的拒绝只朝反方向生效）。**E 到 HOST 门前停住**：HOST ownership 未冻结，封它们等于往未决定的 ownership 里加读数。读数见[第五刀记录](p0-repo-four-case-run-2026-09-26.md)。
- [x] `DONE`（2026-09-26，M 主控轮次）：**E 提出的七项阻断逐项分类，并落第一张工程卡** `INT-EVIDENCE-INTEGRITY-001`。上一行末「待主控处置的四条」就此有答复：① 台账有 `SEALED` 行而 bundle 缺失被静默忽略 ⇒ **普通工程缺陷，已实现**（`tools/report_promotion.py` 的 `discover()` 补上台账→bundle 方向，报告新增 `evidence.sealed_without_bundle`；红先量：删早期 attempt 的字节后报告照出、`promotable` 不变、无字段点名；绿后完整规范卷读数一字未动 `attempts 65 / bundles 101 / sealed_without_bundle []`，切片副本 `bundles 9 / 具名 58 行 / 仍可读 rc=1` ⇒ 非恒空也非拒读）。**故意不拒读**：有意只拷一部分的可逆切片与真丢失在盘上同形，拒读会拆掉卷外复读通道，是否升级为门禁条件属 `P0-GATE-PROMOTION-001`。② 受控镜像无钉住 pytest ⇒ 排 `H1a`，并把第五刀 §5 那一格定成两条都要（镜像补工具链 + 已封 repo bundle 的 `environment` 段按封存进程记，不重封）。③ `domain.sh:657` 静默 `rc=2` 与 ④ runner 未 `import sys` ⇒ 合成 `H1b`（两提交）。⑤ GLFW `[0x1000E]` ⇒ 观察项，再量到才开诊断卡。⑥ `REQUIRED_CASE_NOT_REGISTERED`/`NO_MANDATORY_CASES` 与 `mandatory` 晋级 ⇒ **主控保留**，本轮不翻 registry、不改 `status/gaps`。⑦ HOST 13+2 条 ⇒ §5 所有权未冻结，维持 `DEFERRED`。E 的下一件受控验证登记为 `E-CO`（镜像内自检重封一轮，依赖 `H1a`）。四态：**E 的 `f632665` 与 `759125f`（本卡实现）都已合入 `main`，远端 SHA 逐笔核过并记在[交接](qoder-execution-handoff.md)**；本轮没有新的真跑封证；`H1a`/`H1b`/`E-CO` 尚未验证。
- [x] `DONE`（2026-09-26，M 主控第二轮）：**三张 lane 工程卡按依赖顺序逐张合入 `main`，逐笔核过远端 SHA**。`H1b RUNNER-NAMED-FAILURE-001` ⇒ `d09e9b2`（`domain.sh` 加入基线读取改为双向具名形式：目录存在但不可列 ⇒ 具名理由 + `exit 2`，目录不存在 ⇒ 明说「基线为空」）；`H1a TEST-ORCHESTRATOR-PINNED-PYTEST-001` ⇒ `ee0a439`（受控镜像 `/opt/minekin` venv 带上锁文件钉住的 `pytest==9.1.1` 与四条依赖，配套一条漂移守卫契约测试）；`S2 V1201-MAX-BYTES-VALIDATION-001` ⇒ `ea5423e`（`require_spendable_budget` 在两处入口最先跑，`session start --profile --max-bytes N` 由「接受并忽略」变具名 `exit 2`）。M 侧实测的红绿：旧镜像 `import pytest` ⇒ `ModuleNotFoundError`、`offline-001` 仓库自检 `rc=1` 且五条 `No module named pytest`；新镜像 `pytest 9.1.1` on Python 3.12.3、同一条 `rc=0` `5/5`。漂移守卫两处反证各在一个具名行失败（浮动 pin、把 `9.1.1` 改成 `9.0.0`），反证已还原。**三个合入都不带新的真跑封证**，规范卷全程 `:ro`；合入树全量基础门依次 `2511 / 2512 / 2518 passed`（均 `3 skipped`），ruff check/format、四项 tools 边界脚本、pyright `0 errors` 全绿。**④ 更正**：「runner 使用 `sys` 而未 `import sys`」出自一个未跟踪的 `.tmp/` 探针，M 侧全仓扫描（246 个跟踪 `.py`，两处命中都在字符串字面量内；`test-orchestrator` 无 `.py`；`domain.sh` 那五处 `import json,sys` 齐全）证明**仓库代码未复现**，该条按「未复现」关闭而不是按修复记录。门禁晋级与 `mandatory` 一字未动。读数见[交接](qoder-execution-handoff.md)的 M 第二轮小节。
- [x] `DONE`（2026-09-26，M 主控第三轮）：**两张 lane 卡 + 一张 M 卡按依赖顺序逐张合入，逐笔核过远端 SHA；`E-CO` 的四条真跑封证自此主干可引用**。`6cc9c45` = `E-CO P0-REPO-INTERNAL-IMAGE-RESEAL-001`（E 提交 `626a454`，只动它自己那一份 `docs/p0-repo-internal-image-reseal-2026-09-26.md`）：四条 seq 2 仓库自检 bundle 各 `verify rc=0`、`from_repository_build true`、解释器记 `/opt/minekin/bin/python3`；M 独立只读复核 `attempts 69 / bundles 105 / from_another_build 61 / sealed_without_bundle []`、门载荷 sha256 仍 `fb0152c8…`、`promotable` 仍 `W00/W10/W20/W60` ⇒ **四条 `mandatory: false` 的 PASS 没有写成晋级**。`0ab208c` = **`INT-REPO-CHECK-TOOLCHAIN-PROVENANCE-001`**（M 自己的工程卡，实现 `318a44c`）：读者面新增每包 `check_interpreters` 与名单 `repo_checks_not_from_the_controlled_interpreter`，把「卷上 13 份仓库自检 bundle 里 9 份记的是宿主 `.venv` 解释器（H1a 之前那五次假 FAIL 的形状）」变成可见读数；只命名不门禁（门载荷一字未动、`rc=1` 仍因 `status: blocked`），设计坑由测量纠正（按读者自己的 `sys.executable` 比会给 13/13 全红 ⇒ 改成钉住 runner 镜像契约的常量 + Dockerfile 防漂移测试 + 别名拼写测试）。`be4e79b` = H 的 `V1201-AUTO-PATH-RUNNER-001`（G1/G2，见上一行）。三笔合入树全量门：`2526 passed / 3 skipped`（provenance 树）与 `2528 passed / 3 skipped`（合并 H 后的树），ruff check/format（339→341 文件）、pyright `0 errors`、四项 tools、`bash -n domain.sh`、`git diff --check` 全绿；**M 本轮同样没写规范卷**（全程 `:ro`）。新派两张：`E-RR` 与 H2（下一行）。
- [x] `DONE`（2026-09-27，M 主控第四轮）：**H2 一张卡合入 + ⑤ 的触发条件被卷上字节满足 ⇒ 登记 `H1c`、派 H3**。`6ce9f06` = `V1201-CONTROLLED-SERVER-STATUS-KNOB-001`（H lane；lane 提交 `802f6fc` 实现 `+382/-6`、`24d9142` 记录；base `be4e79b`），把受控启动器写 `server.properties` 的 `enable-status` 从无条件硬编码改成**具名 opt-in + 写后回读 + 落盘前具名拒绝**，**默认仍是 `false`**。M 侧独立双审（不照抄 lane 自报）：改面逐字三处（工具/其契约测试/该 lane 自己那份 `docs/validation/v1201-controlled-server-status-knob-2026-09-27.md`），`test-orchestrator/**`、产品 `src/**`、判据、registry、门禁一字未动；三组反向证明各自转红（旧工具 → `6 failed, 18 passed`；只翻 `DEFAULT_ENABLE_STATUS` → `2 failed, 22 passed` 且指名 `assert True is False`；把拒绝改成先 `return None` → 地址/认证两案红）；M 在自己的 lane 卷里（规范卷连只读都没挂）用同一镜像同一钉住 jar 复量承重读数——默认 ⇒ `enable-status=false` + 产品探针 `NO_RESPONSE` rc=17，`--enable-status` ⇒ `true` + `OBSERVED` / protocol 769 / `received_bytes 163` / rc=0，`--enable-status --online-mode` ⇒ rc=2 具名 `AUTH_MODE_MISMATCH` 且不建 run 目录，与 lane 记录逐字相同。合入树全量 `2534 passed, 3 skipped`；合入后规范卷只读复跑 `attempts 70 / bundles 106 / from_another_build 61 / sealed_without_bundle 0`、门载荷 sha256 仍 `fb0152c8…`、`promotable` 仍 `W00/W10/W20/W60` ⇒ **不搬门、不造「别的构建」、本卡不含真跑封证**。同轮两件新事：① `E-RR` 的第一次重跑封成**具名 FAIL**（`CORE-030` seq 3 `787168062c…`，判据 `NO_CONNECTION_WAS_DIALLED` / `THE_CLIENT_NEVER_DIALLED_A_PORT`，run document `HANDSHAKE_TIMEOUT`，工件带 GLFW `[0x1000E]` 崩溃报告与 `XDG_RUNTIME_DIR is invalid or not set` 一行）⇒ ⑤「再次量到才触发」的条件满足，登记 `H1c`（`QUEUED`，排在 H3 后）；② H2 只做了工具，`domain.sh:565` 仍不传旗标 ⇒ 自动路径仍被 `:638` 的读回后早停掐住，派 **H3 `V1201-AUTO-PATH-STATUS-WIRING-001`**（新支 `codex/minekin-auto-path-status-wiring` @ `../minekin-wt-h3`，base `6ce9f06`）。另登记一条流程更正：M 不得在活动 lane 的 worktree 内做写文件的反证或替 lane 提交（H2 施工中途 M 犯过一次，lane 已按事实重排其提交）。
- [ ] **仅在分支、尚未验证（2026-09-27 第四轮派出）**：① **H3**（H lane，`../minekin-wt-h3`，base `6ce9f06`）：把 `--enable-status` 接进 `domain.sh` 的服务器调用面，**只在 `auto_bundle` 非空这一个谓词下传**（与 `:638` 那条早停同源），非 auto 与黑洞 run 仍不传、读回后早停保留为护栏；只动 `domain.sh` + `tests/contract/test_runner_scripts.py` + 一份新记录，禁改 `tools/**`，禁挂规范卷（此刻 E 在写）；必须先量红再量绿，且如实具名记录「auto run 同时强制 `--online-mode` ⇒ 工具落盘前 `rc=2`」这一族下游事实，不许为了变绿偷偷不传 `--online-mode`。② **`H1c`（`QUEUED`，不占 `lane_next`）**：受控客户端环境具名化（X display / `XDG_RUNTIME_DIR` / GL 后端三态读数 + 区分「客户端从未拨号」与「status 探测不到」两类下游读数），排在 H3 之后，不改判据、不放宽门、不重封两条已封 FAIL。③ **`E-RR`（E lane，进行中，M 只读）**：第一次尝试的 FAIL 已在卷上、材料全留，E 的第二次尝试此刻仍在跑；其记录文档尚未 push ⇒ 文档侧仍是「仅在分支」，M 不代写。**branch 上有实现不等于主干 `DONE`**；push 后由 M 逐卡双审、单张合入并核远端 SHA。
- [ ] **仅在分支、尚未验证（2026-09-26 第三轮派出；其中 H2 已于第四轮合入 `6ce9f06`，见上一行）**：① `E-RR`（E lane，`codex/minekin-evidence` @ `../minekin-wt-evidence`，base `main = be4e79b`）：用修好的 runner 重跑第四刀那次被静默 `rc=2` 掐死的 `CORE-030` 加入尝试，把 ③/G1/G2 从「代码 + 契约测试」升到真跑层证据；写规范卷（E 独占）、只推自己分支，拿不到 PASS 就按 `BLOCKED_HARNESS` 具名上报，不重封既成 attempt、不翻 registry。② `V1201-CONTROLLED-SERVER-STATUS-KNOB-001`（H2，H lane 新支 `codex/minekin-controlled-server-status` @ `../minekin-wt-status`，base `be4e79b`）：`tools/run_controlled_server.py:247` 的 `enable-status` 由硬编码改成显式可命名旋钮，**默认仍是 `false`**，加回读与具名拒绝；不碰规范卷、不碰 `domain.sh`、不改判据。branch 上有实现不等于主干 `DONE`；M 只在 push 后逐卡双审、单张合入并核远端 SHA。
- [ ] **E lane** `NEXT`：`P0-CONTROLLED-CAMPAIGN-001`（主干 `NEXT` 是 M 的 `PARALLEL-INTEGRATION-GATE-001`，本卡在其 §2 激活表里归 E 独立分支施工）。B 段第二张：在受控 dedicated offline 与必要 LAN 场景补当前 build 的 mandatory sealed bundle。**第一刀（`CORE-040`、`CORE-050`）、第二刀（`OFFLINE-030`）、第三刀（5 条 ADMIT 行）、第四刀（`CORE-030`）与第五刀（`OFFLINE-001/040/050` + `ADMIT-080` 四条仓库自检行）已完成，卡片仍在进行**；卡面上点名的三格（B1-a 两条 mandatory、`OFFLINE-030`、5 条 ADMIT 行）、B1 §7 的 9 条最小内容、以及 B1 §6 三格里的**全部非 deferred 形状**（1 条 + 10 条 + 17 条）至此都已按卡面处理或清到只剩需要决策的行。剩余三件事：(1) B1-b `P0-OFFLINE-090-100-EVIDENCE-CHECK-001` 仍是 `QUEUED_PROPOSED`（缺的是 case 定义与断言，不是只差运行，等主控排期）；(2) `only_another_build` 只剩 deferred 的 `HOST-030/040`，而「判官只需仓库字节」那格剩下的 13 条**全部**属 HOST 家族，两件事的前置都是主控的 HOST ownership 决策，E 不代做；(3) 卡面后半段 L3/L5/L6、崩溃恢复、重启协调、offline identity 与 soak（`ADMIT-010/020/030/050/090/120` 等 31 条仍在 `overall.requirement.absent` 名单里，属产品事实无载体或 case id 拆分，见 B1 §9.3）。待主控处置的**基础设施/判读侧**事项共四条：`domain.sh:657`（确定性、静默 `rc=2`）、第四刀那次不可重现的 GLFW 崩溃、受控镜像是否携带一份钉住的测试工具链（否则仓库自检类检查无法在受控环境内执行）、以及「台账有 `SEALED` 行而 bundle 缺失」被静默忽略是否是一张卡。今天 11 个包的全部 block 字段里没有 `CASE_VERSION_MISMATCH`，剩余阻因是 `REQUIRED_CASE_NOT_REGISTERED` / `NO_MANDATORY_CASES`（主控）。
- [ ] `QUEUED_PROPOSED`（B1 交出，等主控排期）：`P0-OFFLINE-090-100-EVIDENCE-CHECK-001`（甲类：字节已封、缺 case 定义与断言；Dashboard 那半句要登记为材料外边界）。
- [ ] `BLOCKED_DECISION`（B1 交出，缺的是选择不是代码）：账本是否承载 profile/endpoint（`ADMIT-010/020`）、人格未重建的承载（`ADMIT-090`）、oracle/canary containment 来源（`ADMIT-120`、`CORE-080`）、OFF-D/OFF-N 是否进候选集（`OFFLINE-060`）、`identity_revision` 变更事件（`OFFLINE-070` 半句）、封禁是否独立分类（`OFFLINE-080` 半句）、`ADMIT-030/050` 的 case id 拆分。执行侧不自造断言。
- [x] `DONE`（2026-09-26，合并 `be4e79b`）：`V1201-AUTO-PATH-RUNNER-001`（G1/G2，H lane）。`domain.sh` 现在捕获 `--auto-bundle` 并从 Server Profile 推导要启动的版本（不再是空 `launched_version`），封存分支从 run document 的 `auto_bundle.recipe_path` 具名取 recipe——缺失或不可读时具名「no seal was attempted」而不是猜；每次真实服务端运行打印它实际写入的 `enable-status`（true/false/unreadable 三态），auto run 非 `true` 时在起客户端之前具名早停，`--probe-player` 成为默认而 `MINEKIN_DOMAIN_PROBE` 的每条等待/判断门控原样保留（判据集合未动，M 侧核对改动前后该变量的门控计数都是 1）。M 侧独立双审读数：真实 `merge-base` 与主干为 `ea5423e`、改面只有 `test-orchestrator/**` + `tests/contract/test_runner_scripts.py`、两条新契约测试对缺陷副本 `2 failed` 在分支 `19 passed`、lane 工作树六项门 `rc=0`、M 合入树全量 `2528 passed / 3 skipped`。**不含真跑封证**：其容器/真 JVM 红绿读数 M 未复量，端到端 auto-bundle 封证改派 `E-RR`（下一行）。
- [x] `DONE`（2026-09-26，合并 `ea5423e`）：`V1201-MAX-BYTES-VALIDATION-001`（A2 的 N2）。非正预算现在由 CLI 入口最先跑的 `require_spendable_budget` 具名拒止（`[BUDGET_NOT_POSITIVE]`，容器实测 `rc=10`），正预算仍走原有 `SUPPLY_CHAIN` 具名路径（`rc=11` 正对照）；`session start --profile --max-bytes N` 由「接受并忽略」变具名用法错误 `MAX_BYTES_WITHOUT_AUTO_BUNDLE`（`rc=2`），`bundle install --max-bytes 0/-1` 同族崩溃一并收掉。四读为 CLI 探针，不含真跑封证。
- [ ] `BLOCKED_DECISION`：`V1201-DISK-PREFLIGHT-001`（A2 的 N3，磁盘不足的失败类别未冻结）与 `V1201-SRV-RESOLVER-001`（A2 的 N4，是否引入 DNS/SRV 解析）。
- [ ] `BLOCKED_DECISION`：`V1201-DEMO-CASE-FREEZE-001`（A1 的 G3）——融合演示 case 断言哪些事实，含 040/080 两种释放原因在同 run 互斥；须主控冻结，执行侧不自造断言。
- [ ] `BLOCKED_DECISION`：V08 对用户 1.20.1 测试服的一次只读探测和非破坏性入服，须新的本次许可；之后 V09 动作须单独限幅授权。V10 依赖真实 V08/V09。
- [ ] `BLOCKED_DECISION`：HOST §5 三格所有权；PERSIST case 冻结；残留进程处置；数据保留/删除。不要猜。
- [ ] `BLOCKED_EVIDENCE`：`P0-GATE-PROMOTION-001` 与各门晋级——`promotable` 只是机器候选， mandatory 真实证据（现由 `P0-CONTROLLED-CAMPAIGN-001` 承接）补齐前不用单测或旧包替代。**读数已变（2026-09-26）**：`report_promotion` 对 W60 给 `promotable: true / blocks [] / requirement.satisfied: true`，即它那 7 条 mandatory 全部满足（其中 `CORE-040`、`CORE-050` 两条是本卡补的，其余 5 条在 B1 的 baseline 上就已满足）；是否晋级仍是主控决定，不由本读数触发。

## 后续阶段索引

执行计划 B：P0 完整门禁与平台矩阵；C：HOST 真实双客户端和存储/恢复；D：PERSIST 与运维策略；E：Harness/Dashboard、导航/生存、长期自治/记忆、社会交互、单 Kin 完整 demo、远期多 Kin/跨维度。每阶段的依赖、验收和阻断都在[执行计划](development-execution-plan.md#4-完整-minekin-长程任务簿设计先行证据后置)。[产品场景路线图](roadmap.md)仍是范围基准，不把已完成的 P0 层误报为整个项目完成。

本页不再追加逐次历史 run 日志；新卡证据记录在卡或独立 evidence 索引，封存件在规范数据根，摘要和 commit 可追。旧档只读。
