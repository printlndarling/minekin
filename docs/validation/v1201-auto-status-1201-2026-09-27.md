# V1201-AUTO-STATUS-ON-1201-001 — `--enable-status` 接线的 1.20.1 隔离侧活体读数（V lane）

日期：2026-09-27。执行 lane：V（1.20.1 游戏内调试）。
分支：`codex/minekin-v1201-auto-status`，worktree `../minekin-wt-v1201-auto-status`。
基线：`cb4786f`（派卡时的远端 `main`；施工期间远端 `main` 又前进（执行时读到 `332e47e`），本分支
`git merge-base HEAD origin/main` 实读仍为 `cb4786f`，一字未挪）。
改动面：**只有本记录这一个文件**。未动 `test-orchestrator/**`（含 `domain.sh` 与 `Dockerfile`，H1c
正在该面施工）、`tools/**`、产品 `src/**`、`bridge*/**`、`tests/fixtures/**`、任何 registry /
`status/gaps` / `mandatory` / 判据 / 门禁 / 四份主干计划文档。驱动脚本与 jar 全部放在被 gitignore
的 `.tmp/` 里，未提交。
资源隔离（本 lane 私有）：容器卷 `minekin-v2-status`（本卡唯一写入面）、Kin 根 `kin-v2`、
受控镜像 `minekin-runner:local`（image id `b67a4d917306`）。规范卷 `minekin-runner-data`
**全程未挂载**（连 `:ro` 都没有，此刻 `H1c` 可能在读它）；`/src` 在全部四组 run 里都是只读挂载。
本卡一切 run 都发生在自己的卷上，属 **lane 侧活体读数，不是主干可引用的 sealed evidence**；
本卡不产生任何封证。全程未连接、未探测任何外部或用户的 Minecraft 服务器，未打开
`.tmp/local-test-server.txt`，文中不出现任何 IP:端口形式的外部地址。

## 为什么补这一格

主干近三轮合入的 `H1a / H1b / G1/G2 / H2 / H3` 的活体读数**全部量在 1.21.4 那侧**；M 在提交
trailers 里如实写着「1.20.1 recipe × `--enable-status` 未测」与「端到端 auto JOIN 仍未验证」。
V1 卡（`docs/validation/v1201-partial-store-and-join-smoke-2026-09-26.md`）把 1.20.1 完整 JOIN
阻于三件事：bridge 是不可联网获取的 workspace 工件（H-2）、G1 封证通道、G2 status 探测失明；
后两件已由 G1/G2+H2+H3 在主干落地，**第一件没有**。本卡把「1.20.1 × `--enable-status`」量成
真实读数，并如实记录其余两格。

## 通道与形状（四组 run 共用一条驱动）

1.20.1 的 auto 形状 = `--auto-bundle` registry fixture + **1.20.1 的 Server Profile**
`tests/fixtures/runtime-input/controlled-offline-server-1.20.1.json`（schema 2，
`allowed_versions: ["1.20.1"]` 单元素）。`domain.sh` 的版本扫描（合并树 `:419-455`）从该 profile
读出唯一允许版本，受控启动器调用面因此带上 `--version 1.20.1`——这就是「1.20.1 recipe ×
`--enable-status`」在 runner 通道里的真实形状。服务端 jar 走仓库自带具名通道取料（制品 CDN 的
钉摘要校验，不是连接任何 Minecraft 服务器）：

```bash
docker run --rm --entrypoint /bin/bash -w /src -v "${REPO}:/src" -e PYTHONPATH=/src/src \
  minekin-runner:local -lc \
  'python /src/tools/verify_supply_chain.py --version 1.20.1 --max-bytes 120000000 \
   --save-server /src/.tmp/mc-1.20.1-server.jar'
# → ok  com.mojang:server:1.20.1  47791053 bytes
# → Supply chain: OK (7 pinned artifacts still match)     （钉摘要 84194a2f…，与 SERVER_RECIPES 一致）
```

逐字驱动脚本 `.tmp/v2-run.sh`（未提交，gitignore 面；`V2_SHAPE` 选形状，四组共用）：

```bash
#!/usr/bin/env bash
set -uo pipefail
export MINEKIN_HOME=/data PYTHONPATH=/src/src LD_LIBRARY_PATH=/opt/sqlite/lib
export MINEKIN_USERNAME="${MINEKIN_USERNAME:-Kin}"
export MINEKIN_DOMAIN_SECONDS="${V2_SECONDS:-40}"
shape="${V2_SHAPE:?}"
server_profile=/src/tests/fixtures/runtime-input/controlled-offline-server-1.20.1.json
auto_bundle=/src/tests/fixtures/registry/reviewed-tested-bundles.json
bundle_profile=/src/tests/fixtures/runtime-input/bundle-candidate-1.20.1.json
domain=/src/test-orchestrator/runner/domain.sh
session_args=(session start --auto-bundle "${auto_bundle}" --server-profile "${server_profile}")
case "${shape}" in
    preh3-red) domain=/src/.tmp/v2-domain-preh3.sh ;;   # 6ce9f06 原字节，sha1 0eb3f55d…
    green) ;;
    control) session_args=(session start --profile "${bundle_profile}" --server-profile "${server_profile}") ;;
    mismatch) export MINEKIN_DOMAIN_ONLINE_MODE=true ;;
    *) echo "V2: unknown shape" >&2; exit 3 ;;
esac
python -m minekin_core init --kin-id kin-v2 >/tmp/v2-init.log 2>&1 || tail -3 /tmp/v2-init.log
runs_before="$(ls -d /data/server-runs/run-* 2>/dev/null | wc -l)"
echo "V2: shape=${shape} run directories before: ${runs_before}"
bash "${domain}" "${session_args[@]}"; rc=$?
echo "V2: domain.sh rc=${rc}"
echo "V2: server tool log (/tmp/domain-server.log, tail 30):"
tail -n 30 /tmp/domain-server.log 2>/dev/null || echo "(absent)"
latest_run="$(ls -d /data/server-runs/run-* 2>/dev/null | sort -V | tail -1)"
echo "V2: newest run directory: ${latest_run:-none}"
if [ -n "${latest_run}" ] && [ -f "${latest_run}/server.properties" ]; then
    echo "V2: enable-status line read from the run directory itself:"
    grep -n '^enable-status=' "${latest_run}/server.properties" || echo "(no such line)"
fi
runs_after="$(ls -d /data/server-runs/run-* 2>/dev/null | wc -l)"
echo "V2: run directories on the volume after: ${runs_after}"
if [ -f /tmp/domain-session.err ]; then
    echo "V2: session stderr (/tmp/domain-session.err, tail 25):"
    tail -n 25 /tmp/domain-session.err
fi
echo "V2: V-b counters"
echo "V2:   joiner session err file present: $([ -f /tmp/domain-join-session.err ] && echo yes || echo no)"
crashes="$(find /data -path '*crash-reports/*client.txt' 2>/dev/null | wc -l)"
echo "V2:   crash-reports client files under /data: ${crashes}"
hits="$(grep -rl '0x1000E' /data /tmp/domain-session.err /tmp/domain-session.json /tmp/domain-join-session.err 2>/dev/null | wc -l)"
echo "V2:   files containing [0x1000E]: ${hits}"
xdg="$(grep -l 'XDG_RUNTIME_DIR' /tmp/domain-session.err /tmp/domain-join-session.err $(find /data -name 'stderr.log' 2>/dev/null) 2>/dev/null | wc -l)"
echo "V2:   files containing the XDG_RUNTIME_DIR line: ${xdg}"
sessions="$(ls -d /data/kin/kin-v2/run/session/* 2>/dev/null | wc -l)"
echo "V2:   sessions recorded for kin-v2: ${sessions}"
exit "${rc}"
```

宿主侧调用模板（四组只差 `V2_SHAPE`；原始输出按形状存 `.tmp/v2-{red,green,control,mismatch}.log`）：

```bash
export MSYS_NO_PATHCONV=1
REPO="$(cygpath -m C:/Users/darling/Documents/agent_work/minekin-wt-v1201-auto-status)"
docker run --rm --entrypoint /bin/bash -w /src \
  -v "${REPO}:/src:ro" -v "${REPO}/.tmp/mc-1.20.1-server.jar:/server/server.jar:ro" \
  -v minekin-v2-status:/data -e MINEKIN_HOME=/data -e PYTHONPATH=/src/src \
  -e V2_SHAPE=<preh3-red|green|control|mismatch> -e V2_SECONDS=30 \
  minekin-runner:local -lc 'bash /src/.tmp/v2-run.sh'
```

同卷按 red→green→control→mismatch 顺序跑（run 目录只增不改，计数可核对）；`green` 起每轮开头
`init` 都打出既有的具名拒绝「/data/kin/kin-v2/kin.sqlite3 already exists…」，那是驱动对同一根的
幂等重初，非读数，台账沿用 red 轮建好的那一根。

## V-a：1.20.1 形状下受控启动器实际拿到的 enable-status

### 红（base 之前的形状：不传旗标的 `domain.sh` 原字节重放）

`.tmp/v2-domain-preh3.sh` = `git show 6ce9f06:test-orchestrator/runner/domain.sh`，
sha1 实读 `0eb3f55dfa10d686553ff1da8f514e5f5266f198`，与 H3 记录点名的 base 缺陷脚本逐字同一枚。
原始输出 `.tmp/v2-red.log`：

```text
V2: shape=preh3-red run directories before: 0
domain: server run directory /data/server-runs/run-1
domain: server ready
domain: the controlled server reports enable-status=false
domain: the auto path needs this server to answer status and it reports enable-status=false; the controlled-server tool has to make it answer (registered separately), so the run stops before the client starts
V2: domain.sh rc=2
enable-status: asked for false, the settings written say false
V2: newest run directory: /data/server-runs/run-1
12:enable-status=false
V2: run directories on the volume after: 1
V2:   crash-reports client files under /data: 0
```

即：1.20.1 的真 JVM 起服并报 ready（门不是恒绿）；不传旗标时受控启动器**实际**写
`enable-status=false`（回读行与 run 目录第 12 行同值；1.20.1 的 properties 该线在第 12 行，
与 H3 量的 1.21.4 第 14 行不同，如实记下）；auto run 的读回后早停**按设计开火**（rc=2，客户端
未起）。

### 绿（合并树形状：同一驱动、同一 1.20.1 通道，wired `domain.sh`）

原始输出 `.tmp/v2-green.log`：

```text
V2: shape=green run directories before: 1
domain: server run directory /data/server-runs/run-2
domain: server ready
domain: the controlled server reports enable-status=true
domain: the session never became playable within 30s (the session exited first)
V2: domain.sh rc=0
enable-status: asked for true, the settings written say true
12:enable-status=true
V2: session stderr (/tmp/domain-session.err, tail 25):
{"category": "SUPPLY_CHAIN", "component": "cli.auto_session", "context": {}, "evidence_ref": null, "message": "3639 of 3639 artifacts are missing and would cost 738432269 bytes; pass --max-bytes deliberately rather than let a session start download them by accident [BUDGET_UNDECLARED]", "operation": "session start --auto-bundle", "retryability": "OPERATOR_ACTION"}
V2:   crash-reports client files under /data: 0
```

要点三条：

1. 受控启动器**实际拿到** `--enable-status`：自报 `asked for true, the settings written say true`，
   run 目录 `server.properties` 第 12 行实录 `true`；早停护栏在位而未触发（打印的是
   `enable-status=true` 那一行，不是具名早停那一行）。
2. auto 路径跨过早停后**真跑到了 1.20.1 自己的下一具名前沿**：`BUDGET_UNDECLARED`，
   **3639 件 / 738,432,269 字节**——这是 1.20.1 recipe 的具名取件集读数（与 V1 的
   `bundle install --dry-run` 全集 3639 件 / 738,432,269 字节一致），不是 1.21.4 的
   4120 件 / 523,788,383 字节（H3 记录）。形状不同的两个前沿读数各自成立，互不冒充。
3. 前沿的**位置本身**证明 status 读数被用掉了：`auto_session.py` 的具名顺序（docstring 原文
   "Order is the safety property"）是 `require_spendable_budget(:354) → ask(server_profile)(:365)
   → resolve(:366) → require_target_allows_launch → require_reviewed_plan(:372，三摘要门)
   → require_agreeing_facts(:374) → _provision(:376，预算门所在)`。run 到达 `_provision` 的
   具名拒绝，意味着状态探针（`ask`）、按观测版本的自动选择与摘要门都在 1.20.1 的 live status
   应答**之后**通过——G2 时代「探测失明 ⇒ 走不到这里」的那一形状就此在 1.20.1 侧被反证。
   该轮未创建 run document（domain 层打印 `this run is ` 与 `the run document said ` 皆空），
   因此 seal 通道未触发，**本卡不产生封证**，如实记。

### 对照（非 auto，`--profile` 形状，合并树）

原始输出 `.tmp/v2-control.log`（逐字截取承重行）：

```text
V2: shape=control run directories before: 2
domain: server run directory /data/server-runs/run-3
domain: server ready
domain: the controlled server reports enable-status=false
V2: domain.sh rc=0
enable-status: asked for false, the settings written say false
12:enable-status=false
V2: session stderr (/tmp/domain-session.err, tail 25):
{"category": "SUPPLY_CHAIN", "component": "cli.session", "context": {}, "evidence_ref": null, "message": "3638 of 3638 artifacts are not in the store yet, starting with com.mojang:minecraft:1.20.1; fetch them before starting a session", "operation": "start", "retryability": "OPERATOR_ACTION"}
```

旗标没有无条件扩散：谓词仍是 `-n "${auto_bundle}"`（合并树 `domain.sh:575-578`），非 auto run
照旧拿默认 `false`，且按设计不触发 auto 早停；该 run 停在自己的具名前沿（`cli.session` 的
缺件拒绝，3638 件 vs auto 取件集 3639 件，差一件即 store 侧计数口径不同，本卡不追）。

### mismatch（auto + 强制 `--online-mode true`，合并树）

原始输出 `.tmp/v2-mismatch.log`（**没有**为了让上一格变绿而隐瞒不传 `--online-mode`；
`MINEKIN_DOMAIN_ONLINE_MODE` 通道保持完整）：

```text
V2: shape=mismatch run directories before: 3
domain: server run directory /data/server-runs/run-4
domain: the server exited before it reported ready
--enable-status asks the controlled server to answer a status ping, and this run forces the server to require session verification; that is the one shape this tool documents as a server its client must never get into (AUTH_MODE_MISMATCH), and a server that announces itself there is offering the join the case exists to measure a refusal of
V2: domain.sh rc=1
V2: newest run directory: /data/server-runs/run-3
V2: run directories on the volume after: 3
```

即：H2 工具的落盘前具名拒止在 1.20.1 形状下同样开火——`run-4` 只被预留命名、从未被创建
（前后计数都是 3，最新仍是 `run-3`），`domain.sh` 停在 `the server exited before it reported
ready`，rc=1，拒绝原文透出。与 H3 在 1.21.4 量的 mismatch 同形，自此在 1.20.1 侧也有了实读。

**V-a 结论**：1.20.1 recipe × `--enable-status` 从「未测」变为四组真实读数（红/绿/对照/mismatch），
承重面（受控启动器实际拿到的值：auto⇒true、非 auto⇒false）与早停护栏的开火/放行均有原始输出。
M 提交 trailer 的那一格就此在隔离侧补上。

## V-b：1.20.1 侧 ⑤ 崩溃与 65 字节 XDG stderr —— 计数为零，形状到不了

四组 run 的驱动计数（各轮原始输出内嵌，此处汇总，只报形状与计数，不编根因）：

| 轮 | joiner err 文件 | `crash-reports/*client.txt` | 含 `[0x1000E]` 的文件 | 含 `XDG_RUNTIME_DIR` 行的文件 | kin-v2 记录的 session 数 |
| --- | --- | --- | --- | --- | --- |
| preh3-red | no | 0 | 0 | 0 | 0 |
| green | no | 0 | 0 | 0 | 0 |
| control | no | 0 | 0 | 0 | 0 |
| mismatch | no | 0 | 0 | 0 | 0 |

读数解释（全部是可核对的构造事实，非猜测）：

- **没有任何一组走到起客户端 JVM**：红/mismatch 在服务器段就具名终止；绿停在客户端启动之前
  （预算门）；对照停在缺件门。⑤ 的崩溃与 XDG stderr 在 1.20.1 隔离侧**既未复现也未被排除**
  ——本通道量不到它，这是一个诚实的零读数，不是一个 PASS。
- 加入者路径在 1.20.1 上**按构造不可达**，两条具名依据：auto run 不能带加入者
  （`domain.sh:404-407` 原文拒止，rc=2）；而 joiner 通道自己合成的 profile 在本 base 上钉死
  `"minecraft_version": "1.21.4"`（`domain.sh:728-745`，第 739 行），加入者的 `xvfb-run -a` 起跳
  在 `:791`。也就是说「1.20.1 的加入者客户端」在这个 runner 里根本没有起跳面。
  （引用发起点为 joiner 路径；`domain.sh:893-915` 的自起 `Xvfb :77-99` 属会话监督进程环境，
  不是加入者路径，本卡未混淆两者。）
- 即便补掉预算（738 MB / 25 分钟量级）也到不了 JVM：取件集**不含 bridge**（V-c），
  起客户端前的装机步骤会先撞上具名拒止（V-c 已在准备层活体量到该拒止原文）。
  所以本卡**没有**花那 25 分钟——它不会改变 V-b 的零读数性质，这一点按形状写明。
- 不为让这个形状不崩去改 runner：那是 `H1c` 正在施工的面。

## V-c：V1 点名的第一件阻断（bridge 非 https、不可联网获取）在当前 base 上仍然成立

四组互相独立的读数，全部在当前 base `cb4786f` 上量：

1. **fixture 字节未变**：`tests/fixtures/runtime-input/bundle-candidate-1.20.1.json` 的 bridge 记录
   仍是 `"source": "workspace:bridge-1201"`（外加 `source_digest`，无 https URL）；registry 的
   1.20.1 条目 `recipe_digest 8ce43e26…` 与产品自己对该文件的 `_reviewed_digest` 实读一致
   （摘要门因此能过，绿的第 3 点即此）。`git log --oneline e6f3e7a..HEAD -- bridge-1201` 为空
   ——V1 基线以来 bridge 面一字未动。
2. **取件集不含它**：容器内驱动产品代码（`.tmp/v2-vc.py`，原始输出 `.tmp/v2-vc.log`）：
   plan 的 fixed_mods 里 `minekin-bridge source= workspace:bridge-1201/build/libs/minekin-bridge-1201-0.0.0.jar`；
   `fetched_mod_artifact(bridge) → None`；`plan_fetch_set` 共 **3639 件、bridge 不在其中、
   无任何 bridge URL**。
3. **仓库里没有那个 jar**：`git ls-files bridge-1201` 里唯一的 jar 是 gradle wrapper；
   容器内 `Path("/src/bridge-1201/build/libs/minekin-bridge-1201-0.0.0.jar").is_file() → False`。
4. **拒止在准备层活体显形**（`.tmp/v2-vc3.py`，原始输出 `.tmp/v2-vc3.log`）：真实取回同 plan 里
   可取的 mod `fabric-api`（2,137,232 字节，`{'complete': True, 'installed': 1, 'reused': 0,
   'failures': []}`）后再走 `install_fixed_mods`，产品的下一条具名拒止正是：

   ```text
   MinekinError  category: SUPPLY_CHAIN
   message: minekin-bridge has not been built: /src/bridge-1201/build/libs/minekin-bridge-1201-0.0.0.jar is missing
   ```

   （中间步 `.tmp/v2-vc2.py` 同题空 store 时先报的是 `fabric-api is not in the content-addressed
   store yet`——拒止按记录顺序逐件显形，bridge 那条不是被跳过的推断。）

**结论**：H-2 未落地，bridge 仍是不可联网获取的非 https workspace 工件；1.20.1 的端到端完整
JOIN 在本 base 上**仍然阻断**，分类维持 `BLOCKED_HARNESS`。本卡未自造替代下载源、未绕过、
未放宽任何判据；registry 与门禁一字未动。

## 门禁（本 worktree @ `cb4786f` + 本记录，`uv sync --locked --dev` 后全部 `uv run --frozen`）

| 门 | 命令 | rc |
| --- | --- | --- |
| lint | `uv run --frozen ruff check .` | 0（All checks passed!） |
| format | `uv run --frozen ruff format --check .` | 0（344 files already formatted） |
| 类型 | `uv run --frozen pyright` | 0（0 errors, 0 warnings, 0 informations） |
| 边界 | `uv run --frozen python tools/check_boundaries.py` | 0（OK） |
| 判据 | `uv run --frozen python tools/check_case_assertions.py` | 0（140 registered） |
| fixture 摘要 | `uv run --frozen python tools/verify_fixture_digests.py` | 0（OK） |
| 工作流钉 | `uv run --frozen python tools/check_workflow_pins.py` | 0（OK） |
| 空白 | `git diff --check` | 0（干净） |
| 全量 | `uv run --frozen pytest -q` | 0（**2535 passed, 3 skipped in 361.41s**；三个 skip 为主干既有平台跳过 `test_orphans.py:686`、`test_silent_listener.py:123`、`test_tested_provenance.py:354`，与 H2/H3 记录同一组，未新增。其中 `test_tested_provenance.py:354` 的跳过理由本身就是 V-c 的同向读数：`bridge-1201/build/libs/minekin-bridge-1201-0.0.0.jar is not built on this host`） |

（本卡不改任何代码：以上门验的是「这份记录提交没有碰任何受检面」。计数与 H3 合并树记录一致。）

## 停在何处（诚实边界 / 本 lane 未量到 / M 侧未复量）

- **端到端完整 JOIN：未达 PASS，按 `BLOCKED_HARNESS` 具名收口**。阻断者按名：bridge 非 https
  不可联网获取（V-c，仍成立）；1.20.1 无加入者起跳面（V-b 的构造事实）；G1/G2/H2/H3 那两件
  已由本卡证实在 1.20.1 通道打开。
- **未测**：全量 738,432,269 字节取件的落地与装后前沿（形状已由 V-c 第 2、4 组读数界定，
  数值本身未在 1.20.1 真下载）；⑤ 在 1.20.1 的复现/排除（通道到不了，零读数≠排除）；
  黑洞分支与 seal 通道的 1.20.1 活体（无 run document，本卡无对象可封）；
  `--enable-status` × 1.20.1 × `--resource-pack` 三合形状。
- **不得被读成**：本卡的绿读数不等于「auto JOIN 可用」；本卡一切卷上内容不是 sealed evidence；
  1.21.4 的 H2/H3 记录未被当作 1.20.1 的结论使用（反之亦然）。
- M 侧对本卡：未复量是常态（协议如此）；本 lane 也请 M 只按原始 `.tmp/v2-*.log` 口径审。

## 四态声明

本记录：**仅在分支**（`codex/minekin-v1201-auto-status`，base `cb4786f`，未合入 main，等待主控双审）。
V-a 四组读数（红/绿/对照/mismatch）与 V-c 准备层拒止：**真实测量**（受控镜像、真 1.20.1 JVM、
本 lane 自建卷 `minekin-v2-status`、逐字驱动脚本）。V-b：**真实零读数**（形状可达性有具名构造
依据，崩溃本身未被量到）。**真实封证：无**——规范卷全程未挂载，本卡不产生任何 sealed
evidence。端到端 auto-bundle 真 JOIN：**仍未验证**（`BLOCKED_HARNESS`，阻断者如上点名）。
本记录不宣称 Minekin 完成，不宣称任何门禁点亮，不宣称已合入 main。
